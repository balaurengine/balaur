// Written by clients/sdkgen (balaur) from the OpenAPI document.
// Do not edit; edit the generator or the spec and run
// clients/generate_balaur.sh.


//! Hooks: one function per operation, as `gamend::hooks::<name>`.
//!
//! A call takes the node its result should reach, then the path's own
//! parameters, then `params` for the request body and `options` for the
//! query where the operation has them. It answers the id `gamend::rest`
//! answers, to await with `task::wait`.


/// Invoke a hook function
/// `POST /api/v1/hooks/call`
pub fn call_hook(node, params) {
    if !gamend::core::given(params, "plugin") {
        return gamend::core::missing("gamend::hooks::call_hook", "plugin");
    }
    if !gamend::core::given(params, "fn") {
        return gamend::core::missing("gamend::hooks::call_hook", "fn");
    }
    gamend::rest(node, "POST", "/api/v1/hooks/call", params)
}

/// List available hook functions
/// `GET /api/v1/hooks`
pub fn list_hooks(node, options) {
    let query = gamend::core::query(options, ["page", "page_size"]);
    gamend::rest(node, "GET", `/api/v1/hooks${query}`)
}
