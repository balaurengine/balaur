// Written by clients/sdkgen (balaur) from the OpenAPI document.
// Do not edit; edit the generator or the spec and run
// clients/generate_balaur.sh.


//! Admin – Notifications: one function per operation, as `gamend::admin_notifications::<name>`.
//!
//! A call takes the node its result should reach, then the path's own
//! parameters, then `params` for the request body and `options` for the
//! query where the operation has them. It answers the id `gamend::rest`
//! answers, to await with `task::wait`.


/// Create a notification (admin)
/// `POST /api/v1/admin/notifications`
pub fn create_notification(node, params) {
    if !gamend::core::given(params, "sender_id") {
        return gamend::core::missing("gamend::admin_notifications::create_notification", "sender_id");
    }
    if !gamend::core::given(params, "recipient_id") {
        return gamend::core::missing("gamend::admin_notifications::create_notification", "recipient_id");
    }
    if !gamend::core::given(params, "title") {
        return gamend::core::missing("gamend::admin_notifications::create_notification", "title");
    }
    gamend::rest(node, "POST", "/api/v1/admin/notifications", params)
}

/// Delete a notification (admin)
/// `DELETE /api/v1/admin/notifications/{id}`
pub fn delete_notification(node, id) {
    gamend::rest(node, "DELETE", `/api/v1/admin/notifications/${id}`)
}

/// List all notifications (admin)
/// `GET /api/v1/admin/notifications`
pub fn list_notifications(node, options) {
    let query = gamend::core::query(options, ["user_id", "sender_id", "title", "page", "page_size"]);
    gamend::rest(node, "GET", `/api/v1/admin/notifications${query}`)
}
