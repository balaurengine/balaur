// Written by clients/sdkgen (balaur) from the OpenAPI document.
// Do not edit; edit the generator or the spec and run
// clients/generate_balaur.sh.


//! Users: one function per operation, as `gamend::users::<name>`.
//!
//! A call takes the node its result should reach, then the path's own
//! parameters, then `params` for the request body and `options` for the
//! query where the operation has them. It answers the id `gamend::rest`
//! answers, to await with `task::wait`.


/// Request an avatar upload ticket
/// `POST /api/v1/me/avatar/upload_url`
pub fn create_current_user_avatar_upload_url(node, params) {
    if !gamend::core::given(params, "content_type") {
        return gamend::core::missing("gamend::users::create_current_user_avatar_upload_url", "content_type");
    }
    gamend::rest(node, "POST", "/api/v1/me/avatar/upload_url", params)
}

/// Delete current user
/// `DELETE /api/v1/me`
pub fn delete_current_user(node, params) {
    gamend::rest(node, "DELETE", "/api/v1/me", params)
}

/// Return current user info
/// `GET /api/v1/me`
pub fn get_current_user(node) {
    gamend::rest(node, "GET", "/api/v1/me")
}

/// Get a user by id
/// `GET /api/v1/users/{id}`
pub fn get_user(node, id) {
    gamend::rest(node, "GET", `/api/v1/users/${id}`)
}

/// Search users by id, username, or display_name
/// `GET /api/v1/users`
pub fn search_users(node, options) {
    let query = gamend::core::query(options, ["q", "page", "page_size"]);
    gamend::rest(node, "GET", `/api/v1/users${query}`)
}

/// Confirm an uploaded avatar
/// `POST /api/v1/me/avatar`
pub fn set_current_user_avatar(node, params) {
    if !gamend::core::given(params, "key") {
        return gamend::core::missing("gamend::users::set_current_user_avatar", "key");
    }
    gamend::rest(node, "POST", "/api/v1/me/avatar", params)
}

/// Update current user's display name
/// `PATCH /api/v1/me/display_name`
pub fn update_current_user_display_name(node, params) {
    if !gamend::core::given(params, "display_name") {
        return gamend::core::missing("gamend::users::update_current_user_display_name", "display_name");
    }
    gamend::rest(node, "PATCH", "/api/v1/me/display_name", params)
}

/// Update current user password
/// `PATCH /api/v1/me/password`
pub fn update_current_user_password(node, params) {
    if !gamend::core::given(params, "password") {
        return gamend::core::missing("gamend::users::update_current_user_password", "password");
    }
    gamend::rest(node, "PATCH", "/api/v1/me/password", params)
}

/// Update current user's username
/// `PATCH /api/v1/me/username`
pub fn update_current_user_username(node, params) {
    if !gamend::core::given(params, "username") {
        return gamend::core::missing("gamend::users::update_current_user_username", "username");
    }
    gamend::rest(node, "PATCH", "/api/v1/me/username", params)
}

/// Player counts
/// `GET /api/v1/users/stats`
pub fn user_stats(node) {
    gamend::rest(node, "GET", "/api/v1/users/stats")
}
