// Written by clients/sdkgen (balaur) from the OpenAPI document.
// Do not edit; edit the generator or the spec and run
// clients/generate_balaur.sh.


//! Admin – Economy: one function per operation, as `gamend::admin_economy::<name>`.
//!
//! A call takes the node its result should reach, then the path's own
//! parameters, then `params` for the request body and `options` for the
//! query where the operation has them. It answers the id `gamend::rest`
//! answers, to await with `task::wait`.


/// Consume items from a user (admin)
/// `POST /api/v1/admin/economy/consume_item`
pub fn consume_item(node) {
    gamend::rest(node, "POST", "/api/v1/admin/economy/consume_item")
}

/// Grant currency to a user (admin)
/// `POST /api/v1/admin/economy/grant`
pub fn grant_currency(node, params) {
    if !gamend::core::given(params, "user_id") {
        return gamend::core::missing("gamend::admin_economy::grant_currency", "user_id");
    }
    if !gamend::core::given(params, "currency") {
        return gamend::core::missing("gamend::admin_economy::grant_currency", "currency");
    }
    if !gamend::core::given(params, "amount") {
        return gamend::core::missing("gamend::admin_economy::grant_currency", "amount");
    }
    gamend::rest(node, "POST", "/api/v1/admin/economy/grant", params)
}

/// Grant items to a user (admin)
/// `POST /api/v1/admin/economy/grant_item`
pub fn grant_item(node, params) {
    if !gamend::core::given(params, "user_id") {
        return gamend::core::missing("gamend::admin_economy::grant_item", "user_id");
    }
    if !gamend::core::given(params, "item") {
        return gamend::core::missing("gamend::admin_economy::grant_item", "item");
    }
    if !gamend::core::given(params, "quantity") {
        return gamend::core::missing("gamend::admin_economy::grant_item", "quantity");
    }
    gamend::rest(node, "POST", "/api/v1/admin/economy/grant_item", params)
}

/// List inventory item stacks (admin)
/// `GET /api/v1/admin/economy/items`
pub fn list_inventory(node, options) {
    let query = gamend::core::query(options, ["user_id", "item", "page", "page_size"]);
    gamend::rest(node, "GET", `/api/v1/admin/economy/items${query}`)
}

/// List ledger entries (admin)
/// `GET /api/v1/admin/economy/ledger`
pub fn list_ledger(node, options) {
    let query = gamend::core::query(options, ["user_id", "currency", "page", "page_size"]);
    gamend::rest(node, "GET", `/api/v1/admin/economy/ledger${query}`)
}

/// List wallets (admin)
/// `GET /api/v1/admin/economy/wallets`
pub fn list_wallets(node, options) {
    let query = gamend::core::query(options, ["user_id", "currency", "page", "page_size"]);
    gamend::rest(node, "GET", `/api/v1/admin/economy/wallets${query}`)
}

/// Spend currency from a user (admin)
/// `POST /api/v1/admin/economy/spend`
pub fn spend_currency(node, params) {
    if !gamend::core::given(params, "user_id") {
        return gamend::core::missing("gamend::admin_economy::spend_currency", "user_id");
    }
    if !gamend::core::given(params, "currency") {
        return gamend::core::missing("gamend::admin_economy::spend_currency", "currency");
    }
    if !gamend::core::given(params, "amount") {
        return gamend::core::missing("gamend::admin_economy::spend_currency", "amount");
    }
    gamend::rest(node, "POST", "/api/v1/admin/economy/spend", params)
}
