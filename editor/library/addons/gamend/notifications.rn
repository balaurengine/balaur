// Written by clients/sdkgen (balaur) from the OpenAPI document.
// Do not edit; edit the generator or the spec and run
// clients/generate_balaur.sh.


//! Notifications: one function per operation, as `gamend::notifications::<name>`.
//!
//! A call takes the node its result should reach, then the path's own
//! parameters, then `params` for the request body and `options` for the
//! query where the operation has them. It answers the id `gamend::rest`
//! answers, to await with `task::wait`.


/// Delete notifications by IDs
/// `DELETE /api/v1/notifications`
pub fn delete_notifications(node, params) {
    if !gamend::core::given(params, "ids") {
        return gamend::core::missing("gamend::notifications::delete_notifications", "ids");
    }
    gamend::rest(node, "DELETE", "/api/v1/notifications", params)
}

/// List own notifications
/// `GET /api/v1/notifications`
pub fn list_notifications(node, options) {
    let query = gamend::core::query(options, ["page", "page_size"]);
    gamend::rest(node, "GET", `/api/v1/notifications${query}`)
}

/// Send a notification to a friend
/// `POST /api/v1/notifications`
pub fn send_notification(node, params) {
    if !gamend::core::given(params, "user_id") {
        return gamend::core::missing("gamend::notifications::send_notification", "user_id");
    }
    if !gamend::core::given(params, "title") {
        return gamend::core::missing("gamend::notifications::send_notification", "title");
    }
    gamend::rest(node, "POST", "/api/v1/notifications", params)
}
