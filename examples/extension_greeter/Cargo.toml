[package]
name = "extension_greeter"
description = "An extension built outside the engine, loaded at run time"
version = "0.1.0"
edition = "2024"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
balaur_plugin = { path = "../../crates/balaur_plugin", version = "0.2.0" }
