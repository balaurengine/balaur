/target
*.bpak
.DS_Store
.idea/
/dist

# The container build context. CI stages this run's editor binaries here, one
# directory per docker architecture, and a local `podman build` does the same.
# Build output, not source.
/docker/dist
__pycache__/
*.pyc
# Local Claude Code state; the web session's hook and its registration are shared.
.claude/*
!.claude/settings.json
!.claude/hooks/
node_modules/
*.vsix
*.profraw
