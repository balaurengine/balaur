> **Status:** planned, 2026-09-03. Nothing here is built yet except the
> pieces the *Have* rows name.

# Plan: showcase media for the manual

One image or one short clip per manual page on balaurengine.org, showing
the feature the page describes, either in the editor or in a running game.
Every image and clip is regenerated by one script from a named editor
state, the way the editor page's screenshots already are, so a change to
the editor's chrome re-takes all of them.

## The list

| Page | Media | Shows | Source |
| --- | --- | --- | --- |
| Scenes and nodes | image + clip | the tree, a node's components in the inspector; clicking through nodes, editing a value, undo | `examples/hello`, Scene persona, `select:Platform` |
| Scripting | image (+ optional clip) | the Script persona: Rune highlighting, hooks, events; hot reload while playing, a breakpoint and a step | `examples/hello`, `scripts/spinner.rn` |
| Animation | clip | Animate persona: scrub, pose a bone, key it, play the loop, the skin deforms | `examples/rig`, `select:Thigh`, `tool:bone` |
| Physics | clip | Physics persona with collider overlays, play, the bird knocks the columns down | `examples/angrynerds`, replayed from a `.blr` |
| UI | image | Interface persona: tree with `Score`, `Birds`, `Hint`, `Restart` widget nodes, the HUD in the viewport, a `widget` in the inspector | `examples/angrynerds`, `ui` persona, `select:Restart` |
| Input | clip | the input overlay: key chips light as keys go down, a ripple at a click, the action names beside the raw keys | `examples/hello` (has `[input.actions]`), playing |
| Networking | none | | |
| Gamend | none | | |
| Modules and extensions | none | | |
| The editor | one image | the whole editor: tree, viewport, inspector, the dock tab row (Output, Problems, Assets, Timeline, Debugger, Session, Profiler) | `examples/angrynerds`, Scene persona, `dock:output` |
| Determinism | clip | play (recording starts), launch a bird by hand, stop; open the session, the lanes show the recorded input; play it back with no hands on the mouse, scrub, the tree and the event rows move | `examples/angrynerds`, Session dock |
| Shaders and materials (new page) | clip | a material in the inspector, its shader in the code editor, a gutter click shows what a line computes, `set_channel` views, save relinks live | `examples/shaders` (new) |
| Shipping a game | none | | |

### Shot by shot

1. **scenes_tree.png** — Scene persona, `examples/hello`, `Platform` selected:
   the tree on the left shows `World` with `Ground`, `Ball`, `Spinner`,
   `Platform`, the two `Crate` instances, `Hud`; the inspector shows
   Transform, `body3d`, `collider3d`, `shape3d`, `animation` and the script
   with its exported `speed`.
2. **scenes_inspect.webm** (~10 s) — the same scene: select `Ball`, then
   `Spinner`, then `CrateB`; the inspector follows; drag `Spinner`'s scale
   up, the viewport changes; `Ctrl+Z`, it changes back.
3. **scripting_editor.png** — Script persona, `spinner.rn` open, the hooks
   and events lists visible. Replaces `persona_script.png`.
4. **scripting_live.webm** (~15 s, optional) — playing; change `speed` in
   the file, save, the spinner speeds up; click line 8's gutter, the game
   stops there, the Debugger dock shows frames and locals, `F10` steps.
5. **animation_key.webm** (~15 s) — Animate persona, `examples/rig`, the
   timeline's lanes are the clip's tracks; scrub the ruler; select `Thigh`
   with the bone tool, drag it, key it; play; the leg swings and the polygon
   deforms. `persona_animate.png` becomes this clip's poster.
6. **physics_collapse.webm** (~10 s) — Physics persona, `examples/angrynerds`,
   collider overlays on; play; the bird launches and the columns and lid come
   down. The launch is a recorded session replayed, so the clip is the same
   every time it is taken.
7. **ui_widgets.png** — Interface persona, `examples/angrynerds`, `Restart`
   selected: the HUD labels and the button over the safe area, the inspector
   showing `kind = button`, `text`, `anchor`, `on_click`. Replaces
   `persona_interface.png`. Needs the button added to the example.
8. **input_overlay.webm** (~10 s) — `examples/hello` playing with the input
   overlay on: press `Space`, `A`, `D`, click; the chips light, the ripple
   lands, `reverse` and `spin` light beside their keys.
9. **editor_overview.png** — Scene persona, `examples/angrynerds`, `Bird`
   selected, the Output dock open, so tree, viewport with the 2D grid and
   gizmo, inspector, status bar and the seven dock tabs are all in one frame.
   The one image on the editor page.
10. **determinism_replay.webm** (~25 s) — `examples/angrynerds`. Play; the
    status bar says it is recording; drag and release the bird; stop. The
    Session dock lists the recording; open it; the `just_pressed` and mouse
    lanes show marks at the ticks the drag happened. Play: the bird launches
    with no hands on the mouse, the input overlay showing the recorded
    pointer; the event rows under the lanes advance with the playhead and the
    tree on the left changes as the game spawns and frees nodes. Scrub the
    slider back, it re-runs from the start.
11. **shader_preview.webm** (~15 s) — `examples/shaders`: select the sprite,
    click the material's shader in the inspector, the `.wesl` opens
    highlighted; click the gutter on the UV line, the noise line and the ramp
    line in turn, the viewport draws each; `set_channel` for normals and
    depth; change a constant, save, the sprite relinks in place.

## How a shot is taken

Have:

- `balaur edit <project> --offscreen --frames N --state "...,shot=<png>"`
  writes one PNG at frame 60 (`editor/scripts/editor.rn`, `render::screenshot`).
- Offscreen frames advance on the fixed 1/60 step, not the wall clock
  (`balaur_render::run_offscreen`), so frame N is always the same picture.
- Start states compose: persona, `select:`, `tool:`, `mode:`, `dock:`,
  `palette`, `light`, `play`, and the `*demo` states in `selftest.rn` that
  script a sequence of edits (`editor/scripts/shell.rn` `states()`).
- Every play-in-editor records a `.blr` under
  `<user data>/sessions/<game>/`; the Session dock replays it, feeding the
  recorded pointer and keys into the game's own input snapshot
  (`editor/scripts/session.rn`).

Planned, in order:

1. **`frames=<dir>[:<from>-<to>]` start state.** Like `shot=` but a
   `render::screenshot(dir/NNNNNN.png)` every frame in the range. A clip is
   then `ffmpeg -framerate 60 -i %06d.png` to `.webm` (VP9) and `.mp4`
   (H.264, for Safari), with frame `from` as the poster PNG. Readback cost
   does not matter: the step is fixed.
2. **Input overlay.** `overlays::input(S)` draws, in a viewport corner, a
   chip per key and mouse button down this frame, the action names that are
   active, and a ripple at the pointer on a press; the pointer itself is
   drawn when a replay is feeding it. On by an editor setting (palette
   command *Show input*, `--state input`). It reads the same
   `input::*` snapshot the game reads, so a replay shows exactly what was
   recorded — that is what clip 10 relies on. A `--show-input` for
   `balaur run` can come later, drawing the same thing over the widget layer.
3. **Showcase states.** `editor/plugins/showcase.rn` registers `show:<name>`
   states, one per clip, each a frame-paced sequence of the calls the UI
   would make (`select`, `set_transform`, `add_key`, `toggle_play`,
   `session::open`, `seek`) with pauses long enough to read, and a drawn
   cursor that moves to what it is about to "click". Same seam as
   `editor/plugins/counter.rn`; `--frames` bounds each one so it is also a
   test. Gameplay inside a clip comes from a committed `.blr` recorded by
   hand once (`examples/<game>/showcase/*.blr`), so no clip needs a human at
   the mouse when it is retaken.
4. **Example content.** `examples/angrynerds`: a `Restart` button widget
   (`on_click = "restart"`). `examples/shaders`: one sprite with a `material`
   whose `.wesl` computes something in three visibly different steps (UV,
   noise, colour ramp), so a gutter click on each line shows a different
   picture.
5. **`scripts/showcase.sh`.** Every image and clip above as one line each —
   project, state, output file — writing to `docs/showcase/`, and the
   website's `scripts/sync-docs.sh` copies them into `static/img/manual/`
   and `static/video/`. No shot is taken by hand.
6. **Website.** MDX takes a plain `<video autoPlay loop muted playsInline
   poster="/img/manual/x.png"><source src="/video/x.webm" /><source
   src="/video/x.mp4" /></video>`; no plugin. Clips are 8–25 s at the
   offscreen size and should stay under ~2 MB each.

## The editor page's existing images

Fifteen images is the page's problem; one overview image is the fix, and
each persona's picture moves to the page about its feature.

| Image | Becomes |
| --- | --- |
| `persona_scene.png` | replaced by `editor_overview.png` |
| `persona_script.png`, `dock_debugger.png`, `save_error.png` | scripting page: `scripting_editor.png` and clip 4 (the save error can stay as one image under *Hot reload*) |
| `persona_animate.png`, `tool_bone.png`, `view3d_bone_tool.png`, `component_limb.png`, `tool_polygon_*.png` | animation page: clip 5's poster plus `component_limb.png` under *Skeletons and skins*; the polygon-tool trio drops to one (`weights`) |
| `persona_physics.png` | physics page: clip 6's poster |
| `persona_interface.png` | UI page: `ui_widgets.png` |
| `palette.png`, `theme_light.png` | dropped; the palette and the theme are a sentence each |

## Docs gaps seen on the way

- There is no manual page for shaders and materials; clip 11 needs one.
- The editor page says four docks; there are seven.
- Prefabs, exported script properties, input actions and save games are in
  the changelog and not in the manual.

## Open questions

- Clip 10 asks the tree to change during the replay. That is spawn and free
  (birds used up), not rename or reorder: those are edits, and a replay
  feeds input, not edits. If the point is "edit the scene, the recording
  still plays", that is a second, shorter clip.
- Whether the input overlay's key chips use the key's own name
  (`Space`, `A`) or the action's; showing both is the plan above.
